version: 2

updates:
  # uv lockfile (uv.lock) — the dedicated ecosystem for uv-managed Python projects.
  - package-ecosystem: "uv"
    directory: "/"
    schedule:
      interval: "weekly"
  # GitHub Actions.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
